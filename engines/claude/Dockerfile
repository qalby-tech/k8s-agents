# claude engine — PLACEHOLDER.
#
# Intended: run Claude Code headless behind a thin control shim that mirrors the
# opencode serve interface, so the controller drives both engines uniformly.
# Build deferred (owner to implement). This stub keeps the directory/tag slot.
#
# When implementing, follow engines/opencode/Dockerfile: bake the `fleet` wrapper
# (COPY shared/fleet/fleet) and shared/AGENTS.md.tmpl, build context = repo root.
FROM busybox:1.36
CMD ["sh", "-c", "echo 'claude engine not implemented yet' && sleep infinity"]
