# agents — retired

This repository is retired as of 2026-09-12.

The images it built (`kamasalyamov/agents:*` — the opencode, claude and codex
engines and the ansible runner) are no longer used by LiveLLM Cloud, and no
LiveLLM Cloud component references them.

The full source is preserved on the branch `archive/full-platform-2026-09` and
the tag `archive-2026-09-12`. Nothing on this branch builds: the workflows are
gone, so pushes here are inert.
